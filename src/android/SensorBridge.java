package org.tonimichelhdm.SensorBridge; import org.apache.cordova.CallbackContext;import org.apache.cordova.CordovaPlugin;import org.apache.cordova.api.PluginResult;import org.json.JSONObject;import org.json.JSONArray;import org.json.JSONException;import android.content.Intent;import android.hardware.Sensor;import android.hardware.SensorManager;public class SensorBridge extends CordovaPlugin {	/*	public static final String ACTION_GET_SENSORLIST = "getSensorList";	public static final String ACTION_GET_ECHO = "getEcho";		@Override	public boolean execute ( String action, JSONArray args, CallbackContext callbackContext) throws JSONEXception {		if(ACTION_GET_ECHO.equals(action)){			String message = args.getString(0);			this.echo(message);			return true;		}		return false;	}		private void echo(String message, CallbackContext callbackContext) {		if( message != null && message.length() > 0) {			callbackContext.success(message + ".. Wow that worked");		}		callbackContext.error("Expected non-empty string argument");	}	*/	}